# This is an example .goreleaser.yml file with some sensible defaults.
# Make sure to check the documentation at https://goreleaser.com

# The lines below are called `modelines`. See `:help modeline`
# Feel free to remove those if you don't want/need to use them.
# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
# vim: set ts=2 sw=2 tw=0 fo=cnqoj

version: 2

project_name: mtrace

git:
  tag_sort: semver

before:
  hooks:
    - go mod tidy -diff
    - go mod download
    - go run main.go gendocs 

builds:
  - id: mtrace-darwin
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X github.com/mtrace-project/mtrace/cmd.Version={{.Version}}
    goos:
      - darwin
    goarch:
      - amd64
      - arm64

  - id: mtrace-linux
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X github.com/mtrace-project/mtrace/cmd.Version={{.Version}}
    goos:
      - linux
    goarch:
      - amd64
      - arm64

  - id: mtrace-windows
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X github.com/mtrace-project/mtrace/cmd.Version={{.Version}}
    goos:
      - windows
    goarch:
      - amd64
      - arm64

gomod:
  proxy: false

archives:
  - id: mtrace-darwin
    ids:
      - mtrace-darwin
    formats: [zip]
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    files:
      - src: "man/*"
        dst: "man"

  - id: mtrace-linux
    ids:
      - mtrace-linux
    formats: [tar.zst]
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    files:
      - src: "man/*"
        dst: "man"

  - id: mtrace-windows
    ids:
      - mtrace-windows
    formats: [zip]
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    files:
      - src: "man/*"
        dst: "man"
report_sizes: true

# Homebrew configuration
homebrew_casks:
  - ids:
      - mtrace-darwin
    manpages:
      - man/*.1
      - "!man/*-completion*.1"

    generate_completions_from_executable:
      executable: "bin/mtrace"
      args:
        - completion
      shell_parameter_format: cobra
      shells:
        - bash
        - zsh
        - fish
        - pwsh
    url:
      # URL which is determined by the given Token (github, gitlab or gitea).
      template: "https://github.com/mtrace-project/mtrace/releases/download/{{ .Tag }}/{{ .ArtifactName }}"
      # Used when the domains of `url` and `homepage` differ.
      verified: "github.com/mtrace-project/mtrace"
      # Tool's website, will become github pages
      cookies:
        license: "accept-backup"
    commit_msg_template: "Brew cask update for {{ .ProjectName }} version {{ .Tag }}"
    homepage: https://github.com/mtrace-project/mtrace
    caveats: "🚀 Let's trace!"
    description: >-
      {{ .ProjectName }} is a CLI tool designed to facilitate testing through trace validation with tracing functionalities on distributed systems, released in version {{ .Tag }}.
    skip_upload: auto
    # Repository to push the generated files to.
    repository:
      owner: mtrace-project
      name: homebrew-tap
      branch: main
      token: "{{ .Env.PKG_PUBLISHER_GITHUB_TOKEN }}"
    commit_author:
      name: mtrace-releaser
      email: alessandro.dinato@gmail.com

# Winget and Scoop configuration
winget:
  - name: mtrace
    ids:
      - mtrace-windows
    publisher: mtrace
    short_description: "CLI tool designed to facilitate testing through trace validation"
    license: "AGPL-3.0"
    publisher_url: https://github.com/mtrace-project
    package_identifier: mtrace.mtrace
    homepage: "https://mtrace-project.github.io"
    description: "mtrace is a CLI tool designed to facilitate testing through trace validation with tracing functionalities on distributed systems."
    tags:
      - golang
      - e2e
      - open-telemetry
      - otel
      - distributed-systems
      - cli
      - tracing
    repository:
      owner: mtrace-project
      name: winget-pkgs
      branch: mtrace-release-{{.Version}}
      token: "{{ .Env.PKG_PUBLISHER_GITHUB_TOKEN }}"
      pull_request:
        enabled: true
        draft: false
        base:
          owner: microsoft
          name: winget-pkgs
          branch: master
    commit_author:
      name: mtrace-releaser
      email: alessandro.dinato@gmail.com

scoops:
  - name: mtrace
    ids:
      - mtrace-windows
    description: "mtrace is a CLI tool designed to facilitate testing through trace validation."
    homepage: "https://mtrace-project.github.io"
    license: "AGPL-3.0"
    repository:
      owner: mtrace-project
      name: scoop-bucket
      branch: main
      token: "{{ .Env.PKG_PUBLISHER_GITHUB_TOKEN }}"
    commit_author:
      name: mtrace-releaser
      email: alessandro.dinato@gmail.com

release:
  github:
    owner: mtrace-project
    name: mtrace
  replace_existing_artifacts: true
  mode: replace

signs:
  - artifacts: checksum
    args:
      - "--batch"
      - "--no-tty"
      - "--pinentry-mode"
      - "loopback"
      - "--passphrase"
      - "{{ .Env.GPG_PASSPHRASE }}"
      - "--output"
      - "${signature}"
      - "--detach-sign"
      - "${artifact}"

changelog:
  sort: desc
  filters:
    exclude: # exludes commits starting with "X:"
      - "^docs" # documentation
      - "^test" # tests
      - "^chore" # small fixes or changes
      - "^ci" # ci/cd
      - "^refactor" # refactoring
      - "^style" # code style
      - "^perf" # performance improvements
      - "^build" # build system changes
      - "^revert" # reverts